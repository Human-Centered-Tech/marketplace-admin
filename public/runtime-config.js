// Replaced at container start by scripts/launch-admin.js with the environment's
// backend URL. The default below keeps a plain `vite preview` / local static
// serve working before that script runs (the app falls back to its build-time
// value when this is empty).
window.__RUNTIME_CONFIG__ = window.__RUNTIME_CONFIG__ || {}
